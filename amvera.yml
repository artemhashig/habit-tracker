meta:
  environment: python
  toolchain:
    name: python
    version: "3.11"
  tool: pip

build:
  requirementsPath: requirements.txt

run:
  command: uvicorn main:app --host 0.0.0.0 --port 80
  persistencePath: /data